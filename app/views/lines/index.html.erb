<h1>Lines</h1>

<ul>
  <% @lines.each do |line| %>
    <li><%= link_to line.number, line_path(line.id) %></li>
  <% end %>
</ul>

<%= link_to "Add Line", new_line_path, :class => "btn btn-default" %>


